spring:
  datasource:
    url: jdbc:mysql://localhost:3306/auth-rococo-db?useSSL=false&serverTimezone=UTC
    username: rococo_admin
    password: rococo_admin123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true